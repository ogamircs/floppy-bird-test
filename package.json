{
  "name": "flappy-bird-ts",
  "version": "1.0.0",
  "description": "Flappy Bird game in TypeScript",
  "main": "dist/main.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "serve": "npx http-server -p 8080 -o",
    "start": "npm run build && npm run serve"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "http-server": "^14.1.1"
  }
}
